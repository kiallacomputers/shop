import { getAdminSupabase, requireAdmin } from "~~/server/utils/adminAuth";
export default defineEventHandler(async(event)=>{ await requireAdmin(event); const q=String(getQuery(event).search||"").trim(); const s=getAdminSupabase(); let b=s.from("sales_customers").select("*,manual_quotes(id,quote_number,status,created_at)").order("created_at",{ascending:false}).limit(300); if(q)b=b.or(`full_name.ilike.%${q}%,company_name.ilike.%${q}%,email.ilike.%${q}%,phone.ilike.%${q}%`); const {data,error}=await b; if(error)throw createError({statusCode:500,statusMessage:error.message}); return data||[]; });
