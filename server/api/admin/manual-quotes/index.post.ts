import { getAdminSupabase, requireAdmin } from "~~/server/utils/adminAuth";
export default defineEventHandler(async(event)=>{await requireAdmin(event);const body=await readBody(event);if(!body?.customer_id)throw createError({statusCode:400,statusMessage:"Select a customer."});const expires=new Date();expires.setDate(expires.getDate()+7);const{data,error}=await getAdminSupabase().from("manual_quotes").insert({customer_id:body.customer_id,expires_at:expires.toISOString().slice(0,10)}).select("id,quote_number").single();if(error)throw createError({statusCode:500,statusMessage:error.message});return data;});
