import {getAdminSupabase,requireSuperAdmin} from "~~/server/utils/adminAuth";
export default defineEventHandler(async e=>{await requireSuperAdmin(e);const b=await readBody(e);if(!String(b.name||'').trim()||!['inflow','outflow'].includes(b.direction)||Number(b.amount)<0||!b.start_date)throw createError({statusCode:400,statusMessage:'Name, direction, amount and start date are required.'});const row={name:String(b.name).trim(),direction:b.direction,amount:Number(b.amount),start_date:b.start_date,end_date:b.end_date||null,frequency:b.frequency||'once',notes:b.notes||null,active:b.active!==false};const{data,error}=await getAdminSupabase().from('accounting_cash_flow_items').insert(row).select().single();if(error)throw createError({statusCode:400,statusMessage:error.message});return data});
