import{getAdminSupabase,requireSuperAdmin}from"~~/server/utils/adminAuth";export default defineEventHandler(async e=>{await requireSuperAdmin(e);const{data,error}=await getAdminSupabase().from("products").select("id,name,product_code,buy_price_ex_gst,price,stock,images,active").order("name");if(error)throw createError({statusCode:500,statusMessage:error.message});return data||[]})