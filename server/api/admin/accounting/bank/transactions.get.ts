import {getAdminSupabase,requireSuperAdmin} from "~~/server/utils/adminAuth";
export default defineEventHandler(async e=>{await requireSuperAdmin(e);const q=getQuery(e),id=Number(q.bank_account_id);if(!id)throw createError({statusCode:400,statusMessage:'Bank account required.'});const s=getAdminSupabase();const {data,error}=await s.from('accounting_bank_transactions').select('*').eq('bank_account_id',id).order('transaction_date',{ascending:false}).order('id',{ascending:false}).limit(1000);if(error)throw createError({statusCode:500,statusMessage:error.message});return data||[]});
