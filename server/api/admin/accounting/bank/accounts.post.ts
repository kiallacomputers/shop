import {getAdminSupabase,requireSuperAdmin} from "~~/server/utils/adminAuth";
export default defineEventHandler(async e=>{await requireSuperAdmin(e);const b=await readBody(e);const s=getAdminSupabase();const row={name:String(b.name||'').trim(),bsb:String(b.bsb||'').trim()||null,account_number:String(b.account_number||'').trim()||null,accounting_account_id:Number(b.accounting_account_id),opening_balance:Number(b.opening_balance||0)};if(!row.name||!Number.isInteger(row.accounting_account_id))throw createError({statusCode:400,statusMessage:'Name and ledger bank account are required.'});const {data,error}=await s.from('accounting_bank_accounts').insert(row).select('*,accounting_accounts(id,code,name)').single();if(error)throw createError({statusCode:400,statusMessage:error.message});return data});
