import{getAdminSupabase,requireSuperAdmin}from"~~/server/utils/adminAuth";export default defineEventHandler(async e=>{await requireSuperAdmin(e);const supplierId=Number(getQuery(e).supplier_id);if(!supplierId)throw createError({statusCode:400,statusMessage:"Supplier is required."});const{data,error}=await getAdminSupabase().from("accounting_product_suppliers").select("*,products(id,name,product_code,buy_price_ex_gst,price,stock,images)").eq("supplier_id",supplierId).order("is_primary",{ascending:false});if(error)throw createError({statusCode:500,statusMessage:error.message});return(data||[]).map((x:any)=>({...x,product:x.products}))})