import{getAdminSupabase,requireSuperAdmin}from"~~/server/utils/adminAuth";export default defineEventHandler(async e=>{await requireSuperAdmin(e);const s=getAdminSupabase();const{data,error}=await s.from("accounting_product_suppliers").select("*,accounting_suppliers(id,name),products(id,name,product_code,sku,buy_price_ex_gst,price,images)").order("id");if(error)throw createError({statusCode:500,statusMessage:error.message});return data||[]})