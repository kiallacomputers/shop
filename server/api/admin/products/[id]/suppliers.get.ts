import { getAdminSupabase, requireAdmin } from "~~/server/utils/adminAuth";
export default defineEventHandler(async (event) => { await requireAdmin(event); const id=Number(getRouterParam(event,"id")); if(!id) throw createError({statusCode:400,statusMessage:"Product ID is required"}); const {data,error}=await getAdminSupabase().from("accounting_product_suppliers").select("id,product_id,supplier_id,supplier_sku,supplier_product_name,buy_price_ex_gst,is_primary,notes,accounting_suppliers(id,name)").eq("product_id",id).order("is_primary",{ascending:false}).order("id"); if(error) throw createError({statusCode:500,statusMessage:error.message}); return data||[]; });
