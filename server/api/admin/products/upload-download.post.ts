import { getAdminSupabase, requireAdmin } from "~~/server/utils/adminAuth";
const MAX_FILE_SIZE=50*1024*1024;
export default defineEventHandler(async(event)=>{ await requireAdmin(event); const parts=await readMultipartFormData(event); const file=parts?.find((part)=>part.name==="file"&&part.filename); if(!file?.data||!file.filename) throw createError({statusCode:400,statusMessage:"No download file was supplied"}); if(file.data.length>MAX_FILE_SIZE) throw createError({statusCode:413,statusMessage:"Each download file must be 50 MB or smaller"}); const ext=(file.filename.toLowerCase().match(/\.([a-z0-9]{1,10})$/)?.[1]||"file"); const base=file.filename.replace(/\.[^.]+$/,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'').slice(0,70)||"download"; const path=`downloads/${base}-${Date.now()}-${crypto.randomUUID()}.${ext}`; const supabase=getAdminSupabase(); const {error}=await supabase.storage.from("products").upload(path,file.data,{contentType:file.type||"application/octet-stream",cacheControl:"3600",upsert:false}); if(error) throw createError({statusCode:500,statusMessage:"Unable to upload download file"}); const {data}=supabase.storage.from("products").getPublicUrl(path); return {path,url:data.publicUrl,filename:file.filename,size:file.data.length,fileType:ext.toUpperCase()}; });
